/*
* Desc: ETW Trace Controller is mainly used to start/stop session and also can be used to enable/disable provider which registered in a specific session
* APIs:
*   StartTrace: create event trace session, use the Properties to correlate provider
*   ControlTrace: stop event trace session 
*/

#pragma once
#include "ETW_Utils.h"

struct ETWTraceController {

};